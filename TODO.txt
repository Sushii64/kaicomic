- implement [handle] tag
    - it's not done the way i want it in the pages, you or i can change it later
    - [handle=CV]cyanusViator[/handle]
    - will color it with the right color and give it the monospace font
    - feel free to change the syntax however you wish
- in chatlogs, it only colors the [CV] pattern but like Homestuck it should color the whole "cyanusViator [CV]" thing
- maybe bring back the functionality for a page to try to pull the next page for the command link instead of the new functionality where it pulls from the index (presumably)
    - makes for easier testing of updates without having to generate the index 
    - optionally, add a flag to deploy to just generate the index
- make /[page number] redirect to /story/[page number] considering that both work
- improve page styling
- add map
- maybe add dynamically generated opengraph embeds or something
